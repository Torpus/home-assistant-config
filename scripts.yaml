living_room_night_light:
  alias: "Turn on lamp and set timer"
  sequence:
    # Cancel ev. old timers
    - service: script.turn_off
      data:
         entity_id: script.living_room_timer_off
    - service: light.turn_on
      data:
        entity_id: light.living_room
    # Set new timer
    - service: script.turn_on
      data:
        entity_id: script.timer_off
living_room_timer_off:
  alias: "Turn off lamp after 5 minutes"
  sequence:
    - delay:
        minutes: 5
    - service: light.turn_off
      data:
        entity_id: light.living_room

upstairs_hall_night_light:
  alias: "Turn on lamp and set timer"
  sequence:
    # Cancel ev. old timers
    - service: script.turn_off
      data:
         entity_id: script.upstairs_hall_timer_off
    - service: light.turn_on
      data:
        entity_id: light.upstairs_hall
    # Set new timer
    - service: script.turn_on
      data:
        entity_id: script.timer_off
upstairs_hall_timer_off:
  alias: "Turn off lamp after 1 minutes"
  sequence:
    - delay:
        minutes: 1
    - service: light.turn_off
      data:
        entity_id: light.upstairs_hall